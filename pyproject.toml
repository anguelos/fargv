[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[tool.ruff]
line-length = 160

[tool.ruff.lint]
select = ["E", "F", "W"]

[tool.coverage.run]
source = ["fargv"]
[tool.coverage.report]
exclude_lines = [
    "def main_",
    "pragma: no cover",
]
